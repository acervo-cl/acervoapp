services:
  acervo-app:
    build:
      context: .
    ports:
      - "8080:80"
    restart: unless-stopped

  test:
    image: node:22-alpine
    working_dir: /workspace
    volumes:
      - .:/workspace
    command: ["npm", "test"]
