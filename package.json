{
  "name": "acervoapp-dev",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:security": "sh scripts/security-smoke.sh",
    "test:all": "npm test && npm run test:security"
  }
}
